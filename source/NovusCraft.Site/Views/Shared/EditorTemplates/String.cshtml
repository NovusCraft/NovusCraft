@model String

@{
	Layout = null;
}

<div>
	@Html.LabelFor(m => m)
	@Html.TextBoxFor(m => m, new { ViewBag.@class, autoComplete = @ViewBag.autocomplete ?? "off" })
	@if (ViewBag.enabledInlineValidationMessages ?? false)
	{
		@Html.ValidationMessageFor(m => m)
	}
</div>